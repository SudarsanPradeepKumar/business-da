// eslint-disable-next-line no-unused-vars
export default function decorate(block) {
  // hero-homepage is a purely visual block - no JS decoration needed
  // CSS handles the split layout (text left, image right)
}
